// URL del Web App de Google Apps Script que guarda las respuestas
// en tu Google Sheet privada. Ver apps-script/README.md para generarla.
// Mientras esté vacía, el formulario funciona en "modo demo": valida todo
// pero avisa por consola que no hay endpoint configurado.
const SURVEY_ENDPOINT = "";
