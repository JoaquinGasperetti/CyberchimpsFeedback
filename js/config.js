// URL del blob de jsonblob.com donde se guardan las respuestas (publicas).
// Formato: https://jsonblob.com/api/jsonBlob/<id>
// Mientras este vacia, el formulario funciona en "modo demo": valida todo
// pero avisa por consola que no hay almacenamiento configurado.
const STORE_URL = "";
